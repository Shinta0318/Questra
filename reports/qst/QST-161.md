# QST-161 Cloud Migration and RLS Evidence

## Status

**Preparation Complete / Cloud Execution Pending QST-160**

QST-160 hosted Supabase projectが未作成・未認証のため、remote migrationとdatabase-backed RLSは
未実行。未確認を合格へせず、Backlog statusは`Planned`を維持する。

## Implemented

- DB URLを`psql` process argumentへ渡さないsecure RLS runner
- URL形式とsanitized targetだけを確認する`-ValidateOnly`
- remote migration/RLS実行結果を記録するcapture script
- candidate commit、project ref、migration head、test SHA-256、assertion数を持つevidence contract
- QST-160 project refとの一致を要求するstrict cloud verifier
- secret、DB URL、private journey contentの記録禁止guardrail
- cloud execution runbook

## Acceptance Progress

| Acceptance | Status | Evidence |
| --- | --- | --- |
| Required migrations applied | Pending hosted project | `BETA_RLS_EVIDENCE.yaml` |
| Owner RLS checks pass | Pending remote psql | `rls_behavior.sql` |
| Cross-account checks pass | Pending remote psql | strict verifier |
| Sanitized evidence | Ready | capture script and guardrails |

## Validation

- PowerShell parser checks
- Secure URL parser with encoded password and `-ValidateOnly`
- `dart format` and `dart analyze`
- Static evidence verifier passes
- Cloud verifier fails while QST-160 evidence is missing
- Isolated complete synthetic evidence passes cloud verifier
- Existing Flutter and RLS static gates remain green

## Completion

After QST-160 cloud verification:

```powershell
$env:SUPABASE_DB_URL = '<secret-postgresql-url>'
powershell -ExecutionPolicy Bypass -File tools/qst/capture_cloud_rls_evidence.ps1 `
  -ProjectRef '<project-ref>'
dart run tools/qst/verify_cloud_rls_evidence.dart --require-cloud
```

## Next QST

QST-162 Dual Account Persistence Acceptance starts after real cloud RLS evidence passes.
