# QST-386 Dynamic RLS Coverage Manifest

## Result

Implemented; hosted behavior evidence remains pending.

## Changes

- Replaced fixed-list-only confidence with migration-derived coverage for every created `public` table.
- Every table must enable RLS and have either a policy or an explicit client revoke contract.
- The manifest records derived table counts and keeps hosted two-account execution mandatory.
- The first dynamic run found three implicit-deny tables without explicit client privilege revocation.
- Added migration `202609030001_explicit_server_only_table_grants.sql` and behavior assertions for those server-only boundaries.

## Validation

- `dart run tools/qst/verify_dynamic_rls_coverage.dart`: passed for 98 tables.
- Existing named-policy and SQL behavior verifier remains additive rather than replaced.
