# QST-332 Quest Journey Hierarchy Integrity and Atomic Operations

## Status

Implemented / hosted migration deployment pending

## Result

- Task origin、version、deferred状態をDB契約へ追加した。
- 親Mission/Quest/owner不一致をtriggerで拒否する。
- `complete_task_journey` RPCで完了、進捗、event、Stardustをtransaction化した。
- version conflict、未完了dependency、他人のTask、未承認企業支援Taskをfail closedにした。
- 同一Mission内の`reorder_mission_tasks` RPCを追加した。

## Remaining external evidence

- migrationを実Supabaseへ配備する。
- 二アカウントRLS、RPC idempotency、stale version、rollbackを実DBで確認する。
