# QST-259 Gemini Mission Architecture Quality Rebuild

## Status

ImplementedProviderEvaluationPending

Mission品質の中核設計、Flutter接続、承認前draft、クラウド配備、静的解析、全テスト、DB lintは完了した。100件以上の実Gemini品質評価、Android/Web E2E、候補単位の再生成・分割・統合・Task化、DockerまたはCI上の二アカウントSQL実行が残るため、正式完了にはしていない。

## Implemented

- Quest UnderstandingとSuccess Contractの後に、Quest固有のAchievement Domain Analysisを追加した。
- Missionを複数Taskが生む中間成果として定義し、必要理由、担当する成功条件、想定Task数、並列可否をStructured Outputへ追加した。
- Mission Granularity Classifierを追加し、`quest / mission / task / too_abstract / duplicate`を区別する契約を実装した。
- Success Contractの必須条件を確認するCoverage Analysisを追加した。
- Criticを10評価軸と明示的な最低点へ強化した。1項目でも閾値未満の候補は表示前に不合格となる。
- 不合格候補と構造・Coverage不足だけを最大2回Repairし、合格候補を維持するPipelineへ更新した。
- サーバー側Semantic ValidatorへTask粒度、Quest言い換え、重複、Success Contract未対応の検出を追加した。
- Flutterの本番生成経路を旧`arc-quest-guide`から`quest-planning-v2`へ切り替えた。
- Gemini障害時に本番で固定Missionテンプレートへ退避せず、入力を保った再試行メッセージを返すようにした。
- Mission候補画面へ中間成果、完了条件、必要理由、必須/任意、想定Task数、前提Mission、並行可否を表示した。
- ユーザー承認時だけ`approve_quest_plan_preview`を呼び、Route・Mission・初期Taskを一括保存するようにした。
- `mission_plan_drafts`、`mission_candidates`、`mission_plan_feedback_events`を追加し、正式Missionと候補を分離した。
- 採用、編集、削除の候補フィードバックを記録するRPCと、plan承認状態を同一トランザクション内で同期するtriggerを追加した。
- 既存の進捗イベントRPCが存在しない`missions.owner_id`を参照していた問題を、Quest joinによる所有者確認へ修正した。

## Cloud Deployment

- Supabase project: `dhbmgwarnrtelrcrmmfk`
- Applied `202608010024_mission_architecture_quality.sql`.
- Applied `202608010025_progress_event_mission_owner_fix.sql`.
- Deployed `quest-planning-v2` with the QST-259 schemas, prompts, validators, and pipeline.

## Verification

- `flutter analyze`: passed with no issues.
- `flutter test`: passed, 370 tests.
- QST-259 focused regression: passed, 20 affected tests.
- Supabase Edge Function deployment and TypeScript bundling: passed.
- Linked Supabase DB lint: passed with zero schema errors after QST-259 follow-up fix.
- RLS readiness verifier: passed for 40 tables and 51 required policies.
- Two-account SQL fixture now includes Mission draft and candidate owner/cross-owner assertions.

## Remaining Release Gates

- Run at least 100 provider-backed Quest cases and prove Task separation accuracy `>= 95%`, non-template quality `>= 90%`, and zero critical safety violations.
- Add and verify candidate-only regenerate, split, merge, and convert-to-Task actions without changing passing Missions.
- Run the two-account SQL suite in Docker/CI. The local CLI attempt was blocked because Docker Desktop was not running.
- Record Android and Web E2E for generation, review, edit/delete, approval, idempotent resubmission, and rollback on failure.
- Reduce normal free-tier planning to approximately three grouped Gemini calls while preserving independent generation and Critic boundaries.

These gates prevent QST-259 from being marked formally complete.
