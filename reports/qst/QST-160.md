# QST-160 Supabase Beta Project Bootstrap

## Status

**In Progress: cloud project evidence pending**

ローカルbootstrap contractは実装済み。Supabase hosted projectのproject ref、region、owner、
migration、Edge Function、secret-name証跡が未取得のため、QST-160を完了扱いにはしない。

## Implemented

- Supabase CLIの現行形式に合わせた`supabase/config.toml`
- 6 Edge Functionの`verify_jwt = true`固定
- Beta用secret exampleとGit除外規則
- dry preflightと明示的`-Apply`を分離したbootstrap script
- project/migration/function/secretのsanitized evidence contract
- static preparationとcloud completionを分離したDart verifier
- hosted projectの作成、link、migration、function deploy、secret登録runbook
- CLI project accessとsanitized Dashboard証跡参照の二重確認

## Acceptance Progress

| Acceptance | Status | Evidence |
| --- | --- | --- |
| Project ref, region, owner | Pending external project | `BETA_SUPABASE_PROJECT.yaml` |
| Migration state | Pending cloud apply | `bootstrap_supabase_beta.ps1` |
| Deployed functions | Pending cloud deploy | 6 function entries |
| Secrets remain server-side | Prepared | ignored env file and name-only evidence |
| Local fallback is not cloud evidence | Enforced | evidence guardrail and `--require-cloud` |

## Validation

- `dart format tools/qst/verify_supabase_beta_bootstrap.dart`
- `dart run tools/qst/verify_supabase_beta_bootstrap.dart`
- Static bootstrap verifier: passed
- `--require-cloud` against repository evidence: expected failure with every missing cloud field listed
- `--require-cloud` against isolated complete synthetic evidence: passed
- Supabase CLI `2.109.1` parsed `config.toml`; status then stopped only because local Docker is not running
- Supabase CLI `init` probe used to confirm current `config.toml` schema
- PowerShell parser and array argument checks: passed
- `git diff --check`

## Completion Command

After the Beta project exists:

```powershell
powershell -ExecutionPolicy Bypass -File tools/qst/bootstrap_supabase_beta.ps1 `
  -ProjectRef "<project-ref>" -Region "<region>" -Owner "<owner>" `
  -DashboardEvidence "<secure-evidence-reference>" -Apply

dart run tools/qst/verify_supabase_beta_bootstrap.dart --require-cloud
```

## Next QST

QST-161 Cloud Migration and RLS Evidence starts only after the cloud verifier passes.
