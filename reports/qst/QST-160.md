# QST-160 Supabase Beta Project Bootstrap

## Status

**Complete: hosted project, migrations, Functions, provider secrets, and Gemini smoke test verified**

ローカルbootstrap contractは実装済み。Supabase hosted project `Questra Beta`を東京リージョンに
作成し、project ref、region、owner、Dashboard accessを確認した。10 migrationsのremote head一致と
6 Edge FunctionのACTIVE / `verify_jwt=true`を確認し、Master Specに従うGemini-first provider層を
実装した。更新版6 Functionsを再配備し、Gemini設定5件をserver-side secretへ登録した。
Gemini Interactions APIの直接疎通と、hosted `arc-chat`からの`gemini_interactions`応答を確認した。

## Implemented

- Supabase CLIの現行形式に合わせた`supabase/config.toml`
- 6 Edge Functionの`verify_jwt = true`固定
- Beta用secret exampleとGit除外規則
- dry preflightと明示的`-Apply`を分離したbootstrap script
- project/migration/function/secretのsanitized evidence contract
- static preparationとcloud completionを分離したDart verifier
- hosted projectの作成、link、migration、function deploy、secret登録runbook
- CLI project accessとsanitized Dashboard証跡参照の二重確認
- stable Gemini Interactions APIを既定とする共有AI provider層
- Gemini Interactions requestの`store=false`固定
- Gemini公式REST endpoint (`v1beta/interactions`)への修正
- 無料枠向けの入力・出力上限、20秒timeout、429/5xxの1回retry
- Flutter Webから呼び出す6 Functions共通のCORS preflight対応
- 固定英語応答だった4 FunctionsのGemini生成と日本語fallbackへの統一
- 明示設定時のみ有効なOpenAI互換経路と、provider未設定時のローカルfallback
- Google API keyを`generativelanguage.googleapis.com`のみに制限
- Secret値を証跡・ログ・Gitへ残さないクラウド検証

## Acceptance Progress

| Acceptance | Status | Evidence |
| --- | --- | --- |
| Project ref, region, owner | Verified in Dashboard | `BETA_SUPABASE_PROJECT.yaml` |
| Migration state | Applied and remote head matched | 10 migration entries |
| Deployed functions | 6 ACTIVE / JWT protected | cloud function list |
| Secrets remain server-side | Verified | 5 provider settings registered; values not recorded |
| Local fallback is not cloud evidence | Enforced | evidence guardrail and `--require-cloud` |
| Gemini direct response | Verified | `completed`, model output parser returned `OK` |
| Hosted Arc Chat response | Verified | `source_type=gemini_interactions`, 3 quick actions |

## Validation

- `dart format tools/qst/verify_supabase_beta_bootstrap.dart`
- `dart run tools/qst/verify_supabase_beta_bootstrap.dart`
- Static bootstrap verifier, Gemini endpoint contract, and shared provider imports: passed
- `--require-cloud` against repository evidence: passed after sanitized evidence update
- `--require-cloud` against isolated complete synthetic evidence: passed
- Supabase CLI `2.109.1` parsed `config.toml`; status then stopped only because local Docker is not running
- Supabase CLI `init` probe used to confirm current `config.toml` schema
- Supabase Dashboard project: `Questra Beta` (`dhbmgwarnrtelrcrmmfk`)
- Primary database region: Northeast Asia (Tokyo), `ap-northeast-1`
- Security defaults: Data API enabled, automatic table exposure disabled, automatic RLS enabled
- Local and remote migration history matched through `202607170002_data_api_privileges.sql`
- 6 Edge Functions deployed as ACTIVE with `verify_jwt=true`
- Provider secrets registered: `AI_PROVIDER`, `GEMINI_API_KEY`, `GEMINI_MODEL`, `AI_MAX_INPUT_CHARS`, `AI_REQUEST_TIMEOUT_MS`
- Gemini direct smoke: `completed`; `thought` and `model_output` steps; extracted response `OK`
- Hosted `arc-chat` smoke: `source_type=gemini_interactions`, non-empty Japanese response, 3 quick actions
- API key restriction: `generativelanguage.googleapis.com` only
- PowerShell parser and array argument checks: passed
- Updated Edge Functions were bundled and deployed successfully by Supabase CLI
- `git diff --check`

## Operations Note

2026-07-18時点ではAI Studioの新しいauth keyで`ACCESS_TOKEN_TYPE_UNSUPPORTED`を再現したため、
内部BetaではGemini API専用に制限したstandard API keyをserver-sideで使用する。Googleの案内に従い、
2026年9月までにauth keyまたはservice-account-bound keyへ移行し、旧keyをローテーションする。

無料枠では入力内容がGoogle製品の改善に利用され得る。内部検証では個人情報・機密情報を入力せず、
外部Beta前にpaid tier、logging、retention、同意文面を再確認する。

## Completion Check

```powershell
dart run tools/qst/verify_supabase_beta_bootstrap.dart --require-cloud
```

## Next QST

QST-161 Cloud Migration and RLS Evidenceへ進む。
